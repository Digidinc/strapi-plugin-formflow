---
"@formflowjs/strapi-plugin-formflow": patch
---

Point upgrade/upsell links and repository metadata to the new Digidinc org after the GitHub org migration. The runtime 402 upgrade URLs, the admin upsell `PURCHASE_URL`, the README, and `package.json` now use `github.com/Digidinc/…` and `digidinc.github.io/formflow`; the retired `hrahimi270.github.io` Pages URL previously left these upgrade links dead.
